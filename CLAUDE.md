# Claude Code instructions

## Context

- อ่าน `docs/PROJECT.md` ก่อนวิเคราะห์ระบบ แก้โค้ด หรือใช้ข้อมูลนักกีฬา
- อ่าน `docs/TRAINING.md` เมื่อสร้างตารางหรือข้อความสั่งซ้อม
- ใช้ `garmin/data/garmin.db` เป็น source of truth ของตัวเลข และใส่ `deleted_at IS NULL` เมื่อ query กิจกรรม
- รักษาขอบเขต Garmin → SQLite → Dashboard ตาม `docs/PROJECT.md`

## Workflow

1. ตรวจ requirement, `git status --short` และโค้ดที่เกี่ยวข้อง
2. แก้เฉพาะขอบเขตที่ขอ พร้อมรักษาการเปลี่ยนแปลงเดิมของผู้ใช้
3. รันเทสที่เกี่ยวข้อง เมื่อแก้ระบบ Garmin ให้รันจาก `garmin/`:
   `.venv\Scripts\python.exe -m unittest discover -s tests`
   Stop hook `.claude/hooks/stop-run-tests.sh` รันให้เองอยู่แล้วเมื่อมี `.py` ใต้ `garmin/`
   เปลี่ยน และ **บล็อกไม่ให้จบเทิร์นถ้าแดง** (549 เทส ~41-49 วิ · ไม่มีอะไรเปลี่ยน = 0.29 วิ)
4. ตรวจ diff และผลทดสอบของตัวเองก่อนสรุป
5. เปิด PR แล้ว **เฝ้า CI เอง** อย่ารอให้ผู้ใช้มาบอกว่าเขียวหรือแดง
   สั่ง `gh run watch` แบบ background แล้วทำงานอื่นต่อได้ ระบบจะปลุกเองเมื่อจบ
6. CI เขียว → merge เข้า main ได้เลยไม่ต้องถาม แล้วลบ branch ในเครื่อง (`git branch -d`)
   ส่วน branch บน GitHub ถูกลบอัตโนมัติ (เปิด `delete_branch_on_merge` ไว้แล้ว
   18 ส.ค. 69 — auto-merge เปิดไม่ได้เพราะ private repo ต้องใช้ GitHub Pro)
   **CI แดง → หยุด รายงานผู้ใช้ อย่า merge และอย่า rerun ทิ้งไปเฉย ๆ** ต้องแยกให้ได้ก่อนว่า
   เป็น flake ของ runner หรือบั๊กจริง (เจอทั้งสองแบบใน 18 ส.ค. 69)

7. **ห้ามวางแผนงานโดยสมมติว่าอีกเทอร์มินอลจะได้รับบรีฟ** — ข้อความข้ามเซสชันต้องให้ผู้ใช้
   กด approve ในหน้าต่างนั้น และมันหมดอายุเองถ้าไม่กดทัน 25 ส.ค. 69 ส่งบรีฟไป 3 ครั้ง
   **หมดอายุทั้งสามครั้ง อีกเซสชันไม่เคยได้รับเลย** แต่ agent วางแผนแบ่งงานไว้แล้วและ
   รอเก้อไปครึ่งเซสชัน → ถ้าจะมอบงาน ให้บอกผู้ใช้ว่าต้องกด approve หรือทำเองไปเลย

งานเสร็จเมื่อ behavior ตรงคำขอ เทสที่เกี่ยวข้องผ่าน และสรุปไฟล์ที่แก้กับข้อจำกัดที่ยังเหลือแล้ว
**ถ้างานนั้นแตะหน้าจอ ยังไม่เสร็จจนกว่าจะรัน `streamlit run` จริงแล้วอ่าน log** (ดูหัวข้อถัดไป)

### ตรวจคำตอบตัวเองก่อนส่งทุกครั้ง

ก่อนส่งข้อความใด ๆ — สรุปงาน โค้ด ใบงาน ข้อเสนอ ตัวเลข — หยุดไล่ 6 ข้อนี้ก่อน
**ข้อไหนตอบไม่ได้ = ยังไม่ส่ง** ให้ไปหาของจริงหรือแก้ให้เสร็จก่อน

| ตรวจ | คำถามที่ต้องตอบได้ |
| --- | --- |
| ถูกต้อง | ตัวเลข ชื่อไฟล์ ชื่อคอลัมน์ คำสั่ง ทุกตัวในข้อความนี้ เห็นมาจากไหน (query, log, diff, ผลเทส) ตัวไหนที่จำมาหรืออนุมานเอา ต้องไปดูของจริงก่อน |
| ครบ | ผู้ใช้ถามกี่ข้อ ตอบครบทุกข้อหรือยัง · ส่วนที่ทำไม่ได้หรือยังไม่ได้ทำ เขียนบอกไว้แล้วหรือยัง |
| มีคุณภาพ | เทสที่เกี่ยวข้องรันแล้วเขียวจริงไหม · ถ้าเป็นข้อเสนอ ครบ 3 ท่อนไหม · มีของที่ผู้ใช้ไม่ได้ขอปนมาไหม |
| พิสูจน์ระดับไหน | คำว่า "พิสูจน์แล้ว/ใช้ได้" ในข้อความนี้ พิสูจน์ถึงชั้นไหน · **`AppTest` เขียว ≠ เบราว์เซอร์ใช้ได้** · ข้อที่พิสูจน์เองไม่ได้ แยกออกมาเขียนบอกแล้วหรือยัง |
| มีทางแก้ไหม | ทุกปัญหา/ข้อจำกัดที่รายงาน มีวิธีแก้ที่เจาะจงพอจะลงมือได้กำกับหรือยัง · ถามผู้ใช้แทนการคิดให้จบอยู่หรือเปล่า |
| อ่านง่าย | ขึ้นต้นด้วยคำตอบหรือยัง · ย่อหน้าไหนที่ควรเป็นตาราง |

รอบนี้ตรวจในหัว **ห้ามเขียนผลการตรวจออกมาให้ผู้ใช้อ่าน** ผู้ใช้ต้องการคำตอบที่ตรวจแล้ว
ไม่ใช่รายงานว่าตรวจอะไรบ้าง เจอผิดให้แก้แล้วส่งของที่ถูก โดยไม่ต้องเล่ากระบวนการ

19 ส.ค. 69 ผู้ใช้ขอเพิ่มรอบนี้เอง · 26 ส.ค. 69 เพิ่มสองแถวล่างหลังผู้ใช้เปิดของจริงแล้วเจอ
error เต็มหน้าจอทั้งที่ agent เขียนว่า "พิสูจน์แล้ว" และหลังผู้ใช้ทักว่า
"ปัญหาที่คุณพลาด แต่ไม่บอกจะแก้ยังไง" — สองแถวนั้นมีเหตุมาจากของจริงแล้ว
ถ้าผ่านไปหลายเซสชันแล้วแถวไหนไม่เคยจับอะไรได้เลย ให้ตัดทิ้งตามเกณฑ์เดียวกับ `/code-review`

### เทสเขียวไม่ได้แปลว่าใช้ได้ — `AppTest` มองไม่เห็นเบราว์เซอร์

26 ส.ค. 69 ผู้ใช้เปิด `run_dashboard.bat` ครั้งแรกหลังรื้อดีไซน์ 6 PR แล้วเจอ
**error เต็มหน้าจอ + กดแท็บแล้วค้าง** ทั้งที่เทส 464 ตัวเขียวและ agent เขียนคำว่า
"พิสูจน์แล้ว" ไปหลายรอบ สามอย่างที่ `AppTest` ผ่านฉลุยแต่ของจริงพัง:

| เคลมที่ผิด | ความจริง | จับได้ด้วยอะไร |
| --- | --- | --- |
| `st.tabs(key=...)` สลับแท็บได้ | ซอร์ส Streamlit เขียน `is_stateful = on_change != "ignore"` **ไม่ส่ง `on_change` = ไม่มี state เลย** `tab.open` เป็น `None` ทุกตัว AppTest เห็นแค่ callback เขียนค่าลง session_state สำเร็จ ไม่ได้บอกว่ามีใครอ่านกลับ | เปิดซอร์สใน `.venv` |
| สีกราฟใน `config.toml` ใช้ได้ | `chartSequentialColors` บังคับ **10 ค่าพอดี** ใส่ 7 ไป มัน log error ทุกครั้งที่เปิดหน้าแล้วถอยไปใช้สีเริ่มต้น | ผู้ใช้เปิดของจริง |
| ฟอนต์ไทยลงธีมแล้ว | ตอนนั้นยังไม่มีใครเห็นมันถูกวาด — 27 ส.ค. 69 ผู้ใช้เปิดดูเองแล้วยืนยันว่าใช้ได้ | ผู้ใช้เปิดของจริง |

**กติกาตั้งแต่นี้ งาน UI ต้องผ่านสี่ชั้น ห้ามข้ามชั้นแล้วเคลม**

1. `unittest` — คุมตรรกะ pure function
2. `AppTest` — คุมว่าสคริปต์รันจบและ element ออกมาครบ **แค่นั้น**
3. **`tests/test_dashboard_theme_config.py`** — เรียก `_populate_theme_msg()` ตัวเดียวกับที่
   Streamlit ใช้ตอนเปิด session แล้วดักทั้ง log และค่าที่รอดเข้าไปถึงหน้าเว็บ
   **เทสนี้เกิดจากบั๊ก 7-vs-10 ข้างบนโดยตรง และยืนยันแล้วว่าแดงกับบั๊กนั้นจริง**
   แตะ `.streamlit/config.toml` เมื่อไหร่ ต้องรันชั้นนี้
4. **บูต `streamlit run` จริง แล้วบังคับให้มันรันสคริปต์หนึ่งรอบ** — ชั้นสุดท้ายก่อนบอกว่าเสร็จ
   ทำอัตโนมัติแล้วโดย Stop hook `.claude/hooks/stop-dashboard-boot.sh` เมื่อ `dashboard.py`
   หรือ `.streamlit/config.toml` เปลี่ยน ใช้เวลา 4.3 วิ สั่งเองได้ด้วย:
   ```
   echo '{}' | .claude/hooks/stop-dashboard-boot.sh   # เงียบ = ผ่าน
   ```

   **สูตรเดิมของชั้นนี้ผิด — `streamlit run` + `grep error` เฉย ๆ แดงไม่เป็น** 27 ส.ค. 69
   ต่อ `import this_module_does_not_exist_xyz` ท้าย `dashboard.py` แล้วมันยังเขียว
   เพราะ **เซิร์ฟเวอร์ไม่แตะสคริปต์เลยจนกว่าจะมี client ต่อเข้ามา** log มีแค่ 6 บรรทัดตอนบูต
   `.claude/hooks/drive_session.py` จึงต่อ websocket เข้า `/_stcore/stream` ส่ง
   `BackMsg(rerun_script)` บังคับให้ ScriptRunner ทำงาน แล้วอ่าน `ForwardMsg` หา element
   ชนิด exception — ยืนยันแล้วว่าจับ `ModuleNotFoundError` ได้ทั้งจาก ForwardMsg และ log
   ส่วนการรอ 40 วิตายตัวก็ไม่ต้องมี เปลี่ยนเป็นรอ `/_stcore/health` ตอบแทน เหลือ 4.3 วิ

**สิ่งที่ไม่มีชั้นไหนพิสูจน์ได้เลย** — แท็บย้ายจริงไหม กราฟอ่านง่ายขึ้นไหม →
**เขียนบอกผู้ใช้ตรง ๆ ว่า "ข้อนี้ผมพิสูจน์เองไม่ได้"** อย่าเขียนรวมกับของที่พิสูจน์แล้ว
ให้ดูน่าเชื่อไปด้วยกัน · 27 ส.ค. 69 ผู้ใช้เปิดดูเองแล้วยืนยันว่า **ฟอนต์ไทยกับ CSS
ใช้ได้ดีแล้ว** สองข้อนี้จึงตัดออกจากรายการ

### แตะอะไรที่กระทบความเร็ว ต้องวัดก่อน-หลัง

25 ส.ค. 69 ใส่ `on_change="rerun"` ให้ `st.tabs` เพื่อให้ปุ่มบนการ์ดสลับแท็บได้
**ไม่ได้วัดอะไรเลย** ผลคือทุกคลิกแท็บกลายเป็น rerun ทั้งสคริปต์ จากเดิมที่เป็นงาน
ฝั่งเบราว์เซอร์ล้วน = 0 วินาที ผู้ใช้เจอทันทีว่า "มันเหลค มันค้าง"

พอไปวัดจริงถึงเห็นว่า **plotly กินไป ~0.7 จาก 1.1 วินาที และสร้างกราฟครบ 14 ใบทุกรอบ
แม้เปิดดูแท็บเดียว** แก้ด้วย `if tab_x.open:` เหลือ 0.39-0.43 วิ บนแท็บที่ไม่มีกราฟ

วัดด้วย `AppTest` + `time.perf_counter()` รอบที่สองเป็นต้นไป (รอบแรกมี cache เย็นปน)
และ `cProfile` เมื่ออยากรู้ว่าเวลาหมดไปกับอะไร — **ทั้งสองอย่างใช้เวลารวมไม่ถึงสองนาที**

### เจอปัญหาแล้วต้องเสนอวิธีแก้ ไม่ใช่โยนคำถามกลับ

26 ส.ค. 69 agent เขียนว่า "มีเรื่องที่ Streamlit ทำไม่ได้จริง ดีไซน์ทำได้แค่ประมาณ"
แล้วจบด้วยการถามผู้ใช้ว่าอยากทำยังไงต่อ ผู้ใช้ตอบกลับว่า
**"ปัญหาที่คุณพลาด แต่ไม่บอกจะแก้ยังไง"**

ทุกครั้งที่รายงานข้อจำกัดหรือความผิดพลาด ต้องมาพร้อม **ทางแก้ที่เจาะจงพอจะลงมือได้
+ ลำดับว่าจะทำอันไหนก่อนและเพราะอะไร** ถามผู้ใช้ได้เฉพาะตอนที่คำตอบเปลี่ยนสิ่งที่จะทำจริง ๆ
ไม่ใช่ใช้คำถามแทนการคิดให้จบ

### เทสต้องผูกกับพฤติกรรม ไม่ใช่รูปร่างของโค้ด

เทสที่ assert ข้อความในซอร์สแบบติดจำนวนช่องอินเดนต์ หรือ assert คอมเมนต์ในไฟล์
จะแดงทุกครั้งที่จัดโครงใหม่ ทั้งที่เจตนายังถูก **เจอ 3 ตัวใน 2 วัน** —
`assertIn("period_metric(\n                wellness_df")` · เทสที่ยืนยันด้วยคอมเมนต์ ·
เทสที่ห้ามใช้ `.open` ซึ่งต้องกลับด้านทั้งข้อภายในสองชั่วโมง

**เจอเทสแบบนี้แดง ห้ามลบทิ้ง ให้ชี้ไปที่พฤติกรรมจริงแทน** เช่นเปลี่ยนจากคอมเมนต์เป็น
"แผงคำตัดสินต้องถูกวาดก่อนอ่าน wellness ค่าแรก" ซึ่งตรวจด้วยลำดับ index จริง
และเวลาจะเขียนยามเฝ้ากฎ **ให้ลองแก้โค้ดให้ผิดกฎดูก่อนว่ามันแดงจริงไหม** ยามที่แดงไม่เป็น
คือยามที่ไม่มีอยู่

### เลือกเส้นทางให้ตรงขนาดงาน

```
รู้แล้วว่าจะแก้อะไร แก้ไม่กี่จุด   →  /tdd  →  commit
ยังไม่ชัดว่าจะทำอะไร ต้องตัดสินใจ  →  /grill-with-docs  →  /tdd
ของพัง + เข้าเงื่อนไขข้างล่าง      →  /diagnosing-bugs
PR แตะ path เสี่ยงข้างล่าง        →  /code-review
```

**`/code-review` ใช้เฉพาะ PR ที่แตะ 3 path นี้** — เขียน DB (`03_backfill.py`,
schema, migration) · token/auth/ACL · สาย backup กับ restore
นอกนั้นไม่ต้องรีวิว งานทั่วไปมี `/tdd` + CI คุมอยู่แล้ว รีวิวซ้ำไม่ได้อะไรเพิ่ม

ข้อนี้ไม่ต้องจำเองแล้ว — PreToolUse hook `.claude/hooks/pre-merge-review-guard.sh`
**deny `gh pr merge` ทิ้งเลย** ถ้า PR แตะ path เสี่ยง (รายชื่อไฟล์มาจาก `git ls-files` จริง
ไม่ใช่คำกว้าง ๆ เพราะ `token` ไปจับ `test_dashboard_chart_tokens.py` ซึ่งเป็น design token)
รีวิวเสร็จแล้วปลดล็อกด้วย `touch .claude/reviewed/pr-<N>`

เดิมเขียนว่า "ก่อน merge เข้า main ทุกครั้ง" ซึ่ง **18 ส.ค. 69 merge 6 PR โดยไม่รีวิวสักครั้ง
และไม่มีอะไรพัง** — กฎที่ถูกเมินทุกครั้งสอน agent ว่าบรรทัดในไฟล์นี้ไม่ต้องทำตามก็ได้
จึงตัดให้เหลือเท่าที่ทำจริงและคุ้มจริง

**`/diagnosing-bugs` ใช้เมื่อเข้าข้อใดข้อหนึ่ง** — ล้มซ้ำหลายรอบโดยยังไม่รู้สาเหตุ ·
แดงบ้างเขียวบ้างที่โค้ดเดิม · เคยดีแล้วพังโดยไม่รู้ว่าพังตอนไหน
**วินัยหลักคือหา "คำสั่งเดียวที่แดงกับบั๊กนี้" ให้ได้ก่อน ห้ามตั้งสมมติฐานก่อนมีของแดง**

18 ส.ค. 69 บั๊ก `_ensure_release` (heartbeat ล้ม 16 รอบติด) เข้าเงื่อนไขเต็ม ๆ แต่ไล่แบบ
ad-hoc จนเจอ — ผลออกมาดีเพราะบังเอิญทำตามวินัยนี้อยู่แล้ว ไม่ใช่เพราะระบบพาไป

**ห้ามเอางานเล็กเข้า `/grill-with-docs`** — 18 ส.ค. 69 งานแก้การแสดงผลสองจุด
(ชื่อรายการ PR กับป้ายฐาน ACWR) ถูกลากเข้าการซัก 4 รอบ จบด้วย ADR และอภิธานศัพท์
ที่ผู้ใช้ไม่ได้ขอและต้องลบทิ้งทั้งหมด ส่วนงานพี่น้องกันที่เข้า `/tdd` ตรง ๆ จบใน 4 รอบ
red-green เกณฑ์คือ **"ตัดสินใจอะไรไม่ได้ถ้าไม่ถาม"** ไม่ใช่ "งานสำคัญแค่ไหน"

`/triage`, `/to-spec`, `/to-tickets`, `/wayfinder` ไม่ใช้ในโปรเจกต์นี้ — คนเดียว
ไม่มี issue ไหลเข้าจากข้างนอก และงานที่นี่ไม่เคยใหญ่ข้ามเซสชัน

### หาข้อเท็จจริงก่อนตัดสินใจ ห้ามเดา

ถ้าคำถามตอบได้ด้วยการดูของจริง (query DB, ยิง Garmin API อ่านอย่างเดียว, อ่านโค้ด
ไลบรารีใน `.venv`) ให้ไปดู อย่าอนุมานจากชื่อฟิลด์หรือความรู้ทั่วไป — เช่นตอนแก้ชื่อ PR
การยิง `get_personal_record()` ดูครั้งเดียวพบว่า `prTypeLabelKey` เป็น null ทุกแถว
ซึ่งตัดแนวทางที่วางไว้ทิ้งทันที ถ้าเดาไปจะแก้ผิดที่

**ข้อนี้ใช้กับคำว่า "ทำไม่ได้" ด้วย และมันแพงกว่าเดาผิดธรรมดา** — 26 ส.ค. 69 agent บอกผู้ใช้ว่า
"Streamlit ให้ได้แค่ sidebar สำเร็จรูป ที่เหลือคือการประมาณ" ซึ่งเกือบทำให้ผู้ใช้ตัดสินใจ
รื้อ tech stack ทั้งโปรเจกต์ พอไป `grep` ซอร์สใน `.venv` จริงถึงเจอว่า
**ทุก widget/container ที่ใส่ `key` ได้คลาส `st-key-<key>` ติดมาด้วย** (`button.py:189`,
`layouts.py:140`) = เจาะจงสไตล์ทีละชิ้นได้ ช่องว่างที่บอกว่าปิดไม่ได้ ปิดได้เกือบหมด
**การเดาว่าเครื่องมือทำอะไรไม่ได้ ทำให้เสนอทางแก้ที่ใหญ่เกินจำเป็น — ต้องอ่านซอร์สก่อนเสมอ**

### ก่อนเสนองาน ถามตัวเองว่า "จำเป็นตอนนี้ไหม"

ทุกข้อเสนอต้องตอบให้ได้ก่อนว่า **มีหลักฐานว่าปัญหานี้เกิดขึ้นจริงแล้วหรือยัง**
ถ้ายังไม่เกิด อย่าเสนอเป็นงานที่ต้องทำ — ให้เสนอเป็น **เงื่อนไขเฝ้าดู** แทน
("ถ้าเจอ 429 ครั้งแรกเมื่อไหร่ ค่อยลด cadence")

18 ส.ค. 69: เสนอปรับปรุงระบบดึงข้อมูล 3 ข้อ พอถูกถามว่า "จำเป็นตอนนี้ไหม" แล้วไปตรวจ
ของจริง เหลือรอดข้อเดียว — ข้อหนึ่งเสนอของที่มีอยู่แล้ว (`uv.lock` ทำ rollback ได้อยู่แล้ว)
อีกข้อแก้ปัญหาที่ไม่เคยเกิด (grep หา 429 ใน log ทุกสาย เจอ 0 ครั้ง)
**ระบบที่ทำงานอยู่ดี การเข้าไปแก้คือการเพิ่มความเสี่ยง ไม่ใช่ลด**

### เสนองานต้องบอกให้ครบ 3 ท่อน

- **ดีขึ้นอะไร** — เขียนในมุมโค้ชที่เปิด Dashboard ไม่ใช่มุมเทคนิค
  ("โค้ชเปิดดูตอน 06:00 จะเห็นการวิ่งเย็นวานแล้ว" ไม่ใช่ "ปิดจุดบอด 13 ชม.")
  ถ้าเขียนประโยคนี้ไม่ได้ = ยังไม่เข้าใจว่ากำลังแก้อะไร
- **ไม่ดีขึ้นอะไร** — พูดตรง ๆ ว่าอะไรที่ยังเหมือนเดิม
- **เสียอะไร** — ต้นทุนและผลข้างเคียง โดยทุกท่อน**มีตัวเลขจริงที่ไปวัดมา**

18 ส.ค. 69 ตอนแก้ `--days 0` → `--days 1`: คำถาม "มีผลกระทบไหม" ทำให้ไปวัดจริงว่า
`get_activities_by_date` รับช่วงวันที่ (= ไม่เพิ่มการเรียก API เลย) และมีกิจกรรม
0 รายการในทั้ง DB ที่จะถูกดึง splits ซ้ำ — ถ้าเดาว่า "น่าจะไม่กระทบ" จะไม่มีหลักฐานอะไรเลย

### ตอบสั้น ใช้ตารางแทนย่อหน้า

ขึ้นต้นด้วยคำตอบหรือตาราง ย่อหน้ายาวใช้เฉพาะตอนอธิบายเหตุผลที่ซับซ้อนจริง ๆ
18 ส.ค. 69 ผู้ใช้ต้องขอ "สั้นกระชับ" ถึง 3 ครั้งในเซสชันเดียว

### DB มีชีวิตตลอดเวลา

Scheduled Task ยิง sync ระหว่างที่ทำงานอยู่ ตัวเลขที่อ่านตอนต้นเซสชันเก่าได้ก่อนจบเซสชัน
(เกิดจริง 18 ส.ค. 69: รายงานว่า "ไม่มีข้อมูลวันนี้" แล้วข้อมูลเข้ามาระหว่างทาง)
เวลารายงานตัวเลขให้ระบุเวลาที่อ่าน และเช็ค `fetched_at` / `MAX(start_time_local)`
ก่อนสรุปว่าข้อมูลขาด

## Agent skills

### Issue tracker

Issues are tracked in this repository's GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single-context domain documentation layout. See `docs/agents/domain.md`.
