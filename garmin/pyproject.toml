[project]
name = "run-performance-garmin"
version = "0.1.0"
description = "Garmin Connect sync tools for Run-Performance-Project (fetch -> SQLite -> Streamlit dashboard)"
requires-python = ">=3.14,<3.15"
dependencies = [
    "garminconnect==0.3.9",
    "streamlit>=1.59.2",
    "pandas>=3.0.3",
    "plotly>=6.9.0",
]

[tool.uv]
package = false
