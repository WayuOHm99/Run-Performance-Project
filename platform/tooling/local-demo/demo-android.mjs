// `demo:android` — launches the Android Emulator surface against the running
// local stack, using the emulator's host-loopback alias. Resets nothing.
// See `launch.mjs`.

import { runLaunch } from "./launch.mjs";

runLaunch("android");
